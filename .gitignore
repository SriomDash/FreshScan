# --- Python Basics ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Virtual Environments ---
venv/
env/
.env
.venv

# --- IDE Settings (VS Code) ---
.vscode/

# --- Jupyter Notebooks ---
.ipynb_checkpoints/

# --- PROJECT SPECIFIC DATA ---

# 1. Ignore the unzipped folder
# (We only need the zip file; we don't want to upload 50+ loose image files)
sample_fruits_50/

# 2. Large Model Files 
# (Optional: If you plan to push to GitHub, uncomment the line below 
# if the model is larger than 100MB. Since yours is ~40MB, you can keep it.)
# *.h5

# 3. Zip files
# (We usually keep this, but if you have other huge zips, ignore them)
# *.zip